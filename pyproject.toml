[project]
name = "md-to-single-html-ebook"
version = "0.1.0"
description = "把 Markdown 小说编译成自包含的单文件 HTML 电子书"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1",
    "markdown-it-py>=3.0",
]

[dependency-groups]
dev = [
    "playwright>=1.40",
]

[tool.uv]
package = false
