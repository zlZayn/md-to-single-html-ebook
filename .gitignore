# ═══════════════════════════════════════════════════════════
# 以下目录永不加入忽略清单：
#   content/  — 书稿源文件，必须入库
#   dist/     — 编译产物，模板的唯一备份源，必须入库
#   templates/ — Jinja2 模板，必须入库
#   docs/     — 设计文档，必须入库
# ═══════════════════════════════════════════════════════════

# Python
__pycache__/
*.py[cod]

# 虚拟环境
.venv/
venv/

# 系统文件
.DS_Store

# 本地工具目录
.workbuddy/
